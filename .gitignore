# Dependencies
node_modules/
.pnpm-store/ # For pnpm

# Next.js build output
.next/
out/

# TypeScript build output
dist/
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
*.log

# Env files (secrets) ในกรณีใช้งานจริงต้องป้องกันไว้ไม่ให้ commit ลง git
# .env
# .env.local
# .env*.local

# Editor / OS junk
.DS_Store
Thumbs.db

# VSCode / history
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
.history/

# Testing / coverage
coverage/

# Optional: ถ้ามีไฟล์ temporary อื่น ๆ
tmp/
temp/

# MongoDB related files (if any local DB files are created)
data/db/
*.sqlite
*.sqlite-journal

# Uploadthing (if used for file uploads)
.uploadthing/

# Other generated files
.turbo/ # Turborepo cache
